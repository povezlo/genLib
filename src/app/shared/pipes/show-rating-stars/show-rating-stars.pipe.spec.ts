import { ShowRatingStarPipe } from './show-rating-stars.pipe';

describe('ShowRatingStarPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowRatingStarPipe();
    expect(pipe).toBeTruthy();
  });
});
